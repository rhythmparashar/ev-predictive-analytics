#configs/trip.yaml
# Driving trip segmentation config

debounce_s: 5
idle_gap_s: 120
min_trip_duration_s: 60

speed_start_rpm: 50
power_start_kw: 1.0

speed_stop_rpm: 20
power_stop_kw: 0.5

# If GAP_INSERTED continues this long, hard-break trips
gap_break_s: 300

# change this